<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="javaFile.mapper.UserMapper">
    <insert id="insertUser" parameterType="javaFile.bean.User">
        INSERT INTO users(password, mobilePhone, createDate, email) VALUES (#{password}, #{mobilePhone}, #{createDate}, #{email});
    </insert>
</mapper>